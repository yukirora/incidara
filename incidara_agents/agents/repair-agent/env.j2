# repair-agent env.j2
# Values from: cfg['common'], cfg['repair-agent'], cfg['<other-service>']

# --- Identity ---
AGENT_NAME={{ cfg['repair-agent']['AGENT_NAME'] }}
MCP_SERVER_ROLE={{ cfg['repair-agent']['MCP_SERVER_ROLE'] }}
CLAUDE_SKILL_DIR={{ cfg['repair-agent']['CLAUDE_SKILL_DIR'] }}
MAX_TURNS={{ cfg['repair-agent']['MAX_TURNS'] }}
PERMISSION_RULES={{ cfg['repair-agent']['PERMISSION_RULES'] }}

# --- LTP ---
LTP_HOST_ADDR={{ cfg['repair-agent']['LTP_HOST_ADDR'] }}
LTP_USER_TOKEN={{ cfg['repair-agent']['LTP_USER_TOKEN'] }}

# --- SSH ---
SSH_TIMEOUT={{ cfg['repair-agent']['SSH_TIMEOUT'] }}
RESET_SSH_USER={{ cfg['repair-agent']['RESET_SSH_USER'] }}
RESET_SSH_PASSWORD={{ cfg['repair-agent']['RESET_SSH_PASSWORD'] }}

# --- BMC ---
BMC_USER={{ cfg['repair-agent']['BMC_USER'] }}
BMC_PASSWORD={{ cfg['repair-agent']['BMC_PASSWORD'] }}
RESET_BMC_PASSWORD={{ cfg['repair-agent']['RESET_BMC_PASSWORD'] }}

# --- Ticket API ---
TICKET_BASE_URL={{ cfg['repair-agent']['TICKET_BASE_URL'] }}
TICKET_AUTH_ZNSL={{ cfg['repair-agent']['TICKET_AUTH_ZNSL'] }}
TICKET_TIMEOUT={{ cfg['repair-agent']['TICKET_TIMEOUT'] }}

# --- K8s / Kubespray ---
K8S_MASTER_IP={{ cfg['repair-agent']['K8S_MASTER_IP'] }}
K8S_MASTER_USER={{ cfg['repair-agent']['K8S_MASTER_USER'] }}
KUBESPRAY_SERVICE_URL={{ cfg['repair-agent']['KUBESPRAY_SERVICE_URL'] }}

# --- Config pipeline ---
BUNDLES_ROOT={{ cfg['repair-agent']['BUNDLES_ROOT'] }}
DATA_MOUNT={{ cfg['repair-agent']['DATA_MOUNT'] }}
SYS_MOUNT={{ cfg['repair-agent']['SYS_MOUNT'] }}
EXT_MOUNT={{ cfg['repair-agent']['EXT_MOUNT'] }}
KUBESPRAY_DIR={{ cfg['repair-agent']['KUBESPRAY_DIR'] }}
APT_MIRROR={{ cfg['repair-agent']['APT_MIRROR'] }}
PIP_MIRROR={{ cfg['repair-agent']['PIP_MIRROR'] }}

# --- MCP endpoints (read by setup-mcp.sh) ---
MCP_NODE_OPS_OPS_URL=http://{{ cfg['common']['bind_host'] }}:{{ cfg['node-ops-ops']['_deploy']['port'] }}/mcp
MCP_AGENT_FEEDBACK_URL=http://{{ cfg['common']['bind_host'] }}:{{ cfg['agent-feedback']['_deploy']['port'] }}/mcp
MCP_AGENT_EVIDENCE_URL=http://{{ cfg['common']['bind_host'] }}:{{ cfg['agent-evidence']['_deploy']['port'] }}/mcp

